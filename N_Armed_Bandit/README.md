## **Reinforcement Learning: Q-Update Rule (RL with Dr. Noori)**

![Python](https://img.shields.io/badge/Python-3.8%2B-blue)
![NumPy](https://img.shields.io/badge/NumPy-1.21%2B-orange)

این پروژه بخشی از تکالیف درس **یادگیری تقویتی** (Reinforcement Learning) در **مقطع کارشناسی ارشد هوش مصنوعی**، پیاده‌سازی شده است.

---

## 🎯 هدف پروژه

- شبیه‌سازی یک عامل (Agent) در یک محیط گسسته با **5 state** (اتاق).
- هر state نشان‌دهنده یک "اتاق" است که با انتخاب یک اکشن (که معادل انتقال به state دیگر است)، پاداشی از ماتریس `R_table` دریافت می‌شود.
- **State 1** دارای بیشترین پاداش‌هاست (به‌ویژه `R[1][1] = 22`)، بنابراین هدف نهایی این است که عامل یاد بگیرد **به state 1 برود و در آنجا بماند**.

---

## 🧠 ساختار محیط

- پنج state با شماره‌های 0 تا 4.
- هر اکشن `a ∈ {0,1,2,3,4}` معادل انتقال مستقیم به state `a` است.
- **Reward Table (`R_table`)**:

  | State \ Action | 0   | 1   | 2   | 3   | 4   |
  |----------------|-----|-----|-----|-----|-----|
  | **0**          | 1   | 0.2 | 2.5 | 4   | 1.5 |
  | **1**          | 9   | **22** | 17 | 10 | 20 |
  | **2**          | 1   | 0.1 | 3   | 5   | 12 |
  | **3**          | 1.2 | 0.5 | 0.9 | 0.7 | 0.6 |
  | **4**          | 1.5 | 0.1 | 0.2 | 2.1 | 0.7 |

- **استراتژی انتخاب اکشن**: `ε-greedy` 

---

## ⚙️ پارامترهای قابل تنظیم

| پارامتر      | مقدار پیش‌فرض | توضیح |
|--------------|----------------|--------|
| `alpha`      | `0.1`          | نرخ یادگیری (Learning Rate) |
| `gamma`      | `0.9`          | ضریب تنزیل (Discount Factor) |
| `epsilon`    | `0.2`          | احتمال exploration |
| `days`       | `3000`          | تعداد روزهای آموزش |
| `tokens/day` | `50`           | تعداد اقدامات در هر روز |

---

## ▶️ نحوه اجرا

1. محیط مجازی ایجاد کنید (اختیاری اما توصیه‌شده):
   ```bash
   python -m venv .venv```
2. فعال‌سازی محیط (ویندوز - PowerShell):
   ```bash
   .\.venv\Scripts\Activate.ps1
3. نصب وابستگی‌ها:
   ```bash
   pip install numpy
4. اجرای اسکریپت:
   ```bash
   python rl_arm_bandit.py
